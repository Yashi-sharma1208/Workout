{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -r node_modules && npm i --save && npm rebuild bcrypt --build-from-source && node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && npm install --prefix ./../frontend && npm run build --prefix ./../frontend && npm install" 
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.3.0",
    "mongoose": "^8.1.1",
    "nodemailer": "^6.9.9",
    "validator": "^13.11.0"
  }
}
